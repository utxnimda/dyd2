---
description: 操作指令词（发布/打包/…）→ 默认动作；「发布」默认同完整发布（pack+deploy）
alwaysApply: true
---

# FMZ 操作指令（口语 → 动作）

用户说 **一个词或短语** 时，按下表 **默认** 执行；**「仅」「只」「不要」「跳过」** 会收窄范围。人类可读主表：**`README.md` →「操作指令一览」**。

## 指令词 → 助手默认

| 用户说法 | 助手默认执行 |
|----------|----------------|
| **完整发布** / **走发布流程** / **上线** / **发布** | ① `npm run pack` ② `npm run deploy`。**不擅自** `git commit` / `git push`（见下「Git 约束」）；完成后 **提醒** 用户提交 `package.json` / `package-lock.json` 等。 |
| **打包** | 仅 `npm run pack`（可 `-- --yes` 时说明场景）。 |
| **提交** | 仅在用户 **当句明确要求** 时执行 `git add` / `git commit`（范围按用户指示）。 |
| **推送** | 仅在用户 **当句明确要求** 时 `git push`。 |
| **仅发布** / **只部署** / **只上线服务器** | 仅 `npm run deploy`（确认 `release/<fmzReleaseLabel>/` 与 `package.json` 一致）。 |
| **仅打包** | 仅 `npm run pack`。 |
| **仅构建** | 仅 `npm run build`。 |
| **后面再发布** | 仅 `npm run pack`，**不** `deploy`。 |
| **开发** | 仅 `npm run dev`。 |
| **全栈开发** / **开发全服务** | `npm run dev:all` 或 `npm run dev:services:start`（按用户偏好）。 |
| **停开发服务** | `npm run dev:services:stop`。 |

## Git 约束（与全局「不擅自提交」一致）

- **完整发布 / 发布** 的默认流水 **不含** commit/push。
- 用户说 **「完整发布并提交」**「**帮我提交**」「**含 git**」等 → 才可代做 **commit**；**push** 需用户 **明确** 要求（或用户规则另有规定）。
- **`release/`** 在 `.gitignore`， **`deploy` 依赖本机已 pack 的目录**。

## 术语（实现层）

| 词 | 通常指 |
|----|--------|
| **打包** | `npm run pack` |
| **发布（技术步骤）** | `npm run deploy` |
| **发布（用户口语）** | 默认 **pack + deploy**（完整发布），**不是** 只做 deploy |

## 时机

- **马上更新**：用户未说「后面再发布」「暂缓 deploy」→ **完整发布** 在 **pack 之后执行 `deploy`**。
- 未要求打包/发布时 **不要** 擅自 `pack` / `deploy`。

**本地配置**（`.pem`、`deploy.local.env`、AI keys）见 **`README.md` →「本地配置方式」**。
