---
description: FMZ 发布流程顺序（打包 → Git → 远端）；单步执行须用户明示
alwaysApply: true
---

# 发布流程规范

当用户要求 **「发布」「走发布流程」「打包发布」** 等**完整上线**时，默认必须按**严格顺序**执行，**不得省略或颠倒**：

1. **本地打包**：版本递增并完成归档（与仓库一致，例如 `node scripts/bump-patch.mjs` → `npm run build` → `node scripts/pack-release.mjs --yes`，或项目约定的等价 `npm run pack` 流程）。
2. **推送 Git**：将本次发布涉及的提交推到 `origin`（或用户指定的远程），除非用户明确说本次不推或无远程权限。
3. **发布远端**：执行 `npm run deploy`（或脚本约定的 deploy），将 `release/<label>/` 同步到服务器。

**例外**：用户**若只需要其中某一步**，必须在话里**特意说明**（例如：「只 push」「只 deploy、不 bump」「只本地 pack 不提交」）。未说明时，按**完整三步**处理。

**注意**：不要将「只提交 / 只改代码」与「完整发布」混为一谈；未要求发布时不要擅自 deploy 或 bump 版本。
