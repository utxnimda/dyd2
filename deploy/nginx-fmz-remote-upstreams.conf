# AI Chat + Fish Audio 音声（幻化宝音）远端 upstream（http{} 内加载）
# 分离部署时由 pack/deploy 覆盖为 43.160.205.247；音频提取 /__fmz_audio 始终在主站 8789

upstream fmz_ai_agent_upstream {
    server 127.0.0.1:8792;
    keepalive 8;
}

upstream fmz_voice_clone_upstream {
    server 127.0.0.1:8793;
    keepalive 4;
}
