# 本机部署/SSH 配置（每机不同）
#
# 复制为同目录下的 deploy.local.env（已在仓库 .gitignore，勿提交）
# copy deploy\deploy.local.env.example deploy\deploy.local.env
#
# 用法：在仓库根目录 PowerShell 中执行：
#   . ./deploy/load-deploy-env.ps1
# 之后会设置进程级环境变量，供 deploy / 手工 ssh 使用。
#
# 多机预设：密钥放在仓库 token/ 下，主机列表见 deploy/servers.json
#   dianfanbao   → 118.195.150.4（www.dianfanbao.net）
#   tencent-43   → 43.160.205.247
#
# 首次：
#   copy deploy\deploy.local.env.example deploy\deploy.local.env
# 将 .pem 放到 token/（文件名与 servers.json 中 key 一致）。

# 当前部署目标（deploy.mjs / import-remote 会自动读 deploy.local.env）
FMZ_DEPLOY_TARGET=dianfanbao

# 以下会覆盖 servers.json 中同名字段（一般不必写，改 TARGET 即可）
# FMZ_DEPLOY_SSH_KEY=D:/Buf/Test/fmz-dashboard/token/118.195.150.4.pem
# FMZ_DEPLOY_SSH_USER=root
# FMZ_DEPLOY_SSH_HOST=118.195.150.4
# FMZ_DEPLOY_WEB_ROOT=/var/www/fmz-dashboard/

# 部署到第二台机（AI 网关）：
# FMZ_DEPLOY_TARGET=tencent-43

# AI 网关分离：与 43.160.205.247 上 /etc/fmz-ai-gateway.env 相同
# FMZ_REMOTE_SERVICE_SECRET=请填长随机串
# 主站 deploy 时同步 Nginx 上游与密钥片段：
# FMZ_DEPLOY_SYNC_NGINX=1

# 仅部署静态：不向 /opt 同步后端、不 systemctl restart（默认不设）
# FMZ_DEPLOY_SKIP_BACKEND=1

# 将 release/config/nginx 同步到远端并 nginx -t && reload（默认不设；会覆盖 conf.d 同名文件）
# FMZ_DEPLOY_SYNC_NGINX=1
# FMZ_DEPLOY_NGINX_CONF_D=/etc/nginx/conf.d
