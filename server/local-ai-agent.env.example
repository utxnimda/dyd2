# 复制为本目录下的 local-ai-agent.env（已在 .gitignore，勿提交）。
#
# 多厂商密钥也可集中放在 server/data/：复制 ai-agent-keys.example.json → ai-agent-keys.json
#（该 json 已 gitignore，勿提交）。

#

# 「系统代理」「纯 TUN」且无本地监听端口：不要设置 HTTPS_PROXY，保持示例里代理行为注释。

# 「系统代理」由 ai-agent-server 在 Windows 上自动读注册表（环境变量仍会优先）。

# FMZ_AI_AGENT_SKIP_SYSTEM_PROXY=1

#

# OpenAI Chat Completions（与 ai-agent-server 内置 gpt-* 条目一致）；也可写在 server/data/ai-agent-keys.json 的 openai 字段。
# OPENAI_API_KEY=sk-...
# 自建 / Azure OpenAI 兼容网关时按需覆盖（默认为 https://api.openai.com/v1）：
# OPENAI_BASE_URL=https://api.openai.com/v1

# 通义千问 DashScope（OpenAI 兼容）。也可写 ai-agent-keys.json 的 qwen 字段。二选一与环境变量 QWEN_API_KEY 等价：
# DASHSCOPE_API_KEY=sk-...
# 国际域（北京外）常见需改基址：
# QWEN_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

# 试拉 Google list models / Chat 时，优先走本机 HTTP 混合端口（高于 Windows 系统代理）。与 Clash「Mixed / 7890」等一致，按实际端口填写：
# FMZ_AI_AGENT_LOCAL_PROXY=http://127.0.0.1:7890

# 也可用通用写法（与上一行二选一即可；均优先于系统代理）：

#HTTPS_PROXY=http://127.0.0.1:7890

#HTTP_PROXY=http://127.0.0.1:7890

# 仅使用内置模型列表、不请求 Google list models（调试或网络受限时）：
# FMZ_AI_AGENT_MODELS_SOURCE=static

# --- 模型可达性心跳（默认开启）：定时对每台已配置密钥的模型发极小 /chat 探测；失败则从 /models 与 AI 面板隐藏，直至下一轮探测成功 ---
# FMZ_AI_AGENT_PROBE_DISABLED=1
# 探测周期（毫秒，最短 60s；默认 300000）
# FMZ_AI_AGENT_PROBE_INTERVAL_MS=300000
# 单次请求超时（毫秒；默认 22000）
# FMZ_AI_AGENT_PROBE_TIMEOUT_MS=22000
# 两个模型探测之间的间隔（毫秒；减轻上游限流；默认 450）
# FMZ_AI_AGENT_PROBE_STAGGER_MS=450

# 若本机仅「Gemini 2.0」无免费额度 / 总报错 429，可从列表中隐藏（逗号分隔 model id）：
# FMZ_AI_AGENT_DISABLED_MODEL_IDS=gemini-2.0-flash,gemini-2.0-flash-lite

#
# --- 弹幕服务 douyu-danmaku-server（触发器 AI 日报/周报）---
# 与 ai-agent /chat 相同的 modelId；可填多个（逗号分隔），遇上游不可用 / 配额 / HTTP 异常等会自动试下一候选。
# 不设时在本机能从 GET /models 取到列表则按 ai-agent 返回顺序整条链降级（跨厂商）；取不到则用内置 Gemini 默认链。
# FMZ_TRIGGER_AI_MODEL=gemini-2.5-flash,qwen-plus,gpt-4o-mini
# 亦可在已配置 OPENAI_API_KEY 时使用：gpt-4o-mini,gpt-4o
# 已配置 DASHSCOPE_API_KEY 时可写：qwen-plus,qwen-max
